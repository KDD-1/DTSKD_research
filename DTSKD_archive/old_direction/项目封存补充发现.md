# DTSKD 项目封存 — 实验补充发现

> 日期：2026-06-01 | 基于代码审查 + 两次对比实验

---

## 一、Bug 修复记录

### 1.1 HSKD 单GPU 历史预测更新缺失（关键Bug）

**位置**：`main.py` train() 函数末尾

**问题**：原代码仅在 `multiprocessing_distributed=True` 时更新 `all_predictions` 矩阵（用于下一轮历史软目标生成）。单 GPU 训练时 `all_predictions` 始终停留在 epoch 0 初始化的 one-hot 值，导致 HSKD 退化为标准 CE 训练。

**修复**：在 `if args.HSKD:` 分支中补充 `else` 分支，单 GPU 模式下用当前 softmax 输出更新预测矩阵。

**影响**：这是作者 README 中提到的"复现性可能因清理代码而降低"的原因之一。修复后 HSKD 在单 GPU 下正常工作。

### 1.2 `--resume` 参数重复定义

原代码第 80 行和 82 行两次定义 `--resume`，导致 argparse 报错。已删除重复行，保留 `default=''`。

---

## 二、Loss 函数正名

代码中变量名具有误导性，实际语义如下：

| 变量名 | 实际函数 | 温度 | 论文对应 |
|--------|---------|------|---------|
| `loss_ce` | `KL_Loss2(T=1)` | 1 | **L_his**（历史KD） |
| `loss_kd` | `KL_Loss(T=4)` | 4 | **L_stru**（结构KD） |
| `criterion_CE` | `nn.CrossEntropyLoss()` | — | 仅在HSKD=0时使用 |

### 数学公式

```
soft_bk_targets = α_t · one_hot + (1-α_t) · last_bk_prediction

L_his = 3.0 · KL(backbone ‖ soft_bk_targets)      [T=1]
      + 1.0 · KL(branch_1  ‖ soft_b1_targets)      [T=1]
      + 1.0 · KL(branch_2  ‖ soft_b2_targets)      [T=1]
      + 1.0 · KL(branch_3  ‖ soft_b3_targets)      [T=1]

L_stru = KL(branch_1 ‖ backbone)  [T=4]
       + KL(branch_2 ‖ backbone)  [T=4]
       + KL(branch_3 ‖ backbone)  [T=4]

L_total = 0.2 · L_his + 0.8 · L_stru    （实验所用参数）
```

### 梯度流向

- **Backbone** 同时从 L_his（历史监督）和 L_stru（KL teacher 端反向梯度）接收信号
- **Branch** 从 L_his（自身历史）和 L_stru（向 backbone 学习）接收信号
- L_stru 中 teacher 侧未 detach，因此 backbone ↔ branch 存在双向梯度

---

## 三、α_t 崩塌机制验证

### 实验设计

两组对照实验，仅改变 `cos_min` 参数：

| 参数 | 实验A (崩塌) | 实验B (稳定) |
|------|:----------:|:----------:|
| end_epoch | 50 | 50 |
| cos_max | 0.9 | 0.9 |
| cos_min | **0.0** | **0.5** |
| α_t 范围 | 0.9 → 0.0 | 0.9 → 0.5 |
| lr | 0.1（无衰减） | 0.1（无衰减） |

### 结果

| | 实验A (cos_min=0.0) | 实验B (cos_min=0.5) |
|---|---|---|
| 最佳 epoch | 32 | 43 |
| 最佳 Val Top-1 | 56.23% | **56.78%** |
| 最终 Val Top-1 | **33.32%** | **52.64%** |
| 趋势 | 崩塌（↓23%） | 平稳震荡（±5%） |

### 崩塌机制

当 α_t → 0：
1. soft_targets ≈ 模型自己上一轮的预测（可能不准确）
2. Backbone 在 L_his（自我一致性）+ L_stru（分支共识）双重作用下漂移
3. 无真实标签信号注入 → Backbone 退化 → 分支跟随退化 → 正反馈崩塌

α_t ≥ 0.5 可确保历史 KD 始终至少包含 50% 真实标签，阻止此循环。

### 关于 "80% 梯度来自历史教师" 的纠正

之前分析称"80% 来自历史教师"是错误的。实际上：
- `kd_weight=0.8` 对应的是 **结构KD (L_stru)**，使用当前 backbone 输出，不依赖 α_t
- `ce_weight=0.2` 对应的是 **历史KD (L_his)**，受 α_t 控制
- 但当 backbone 因 α_t→0 而退化时，80% 权重依赖的 backbone 教师本身已不可靠，形成连锁崩塌
- 因此瓶颈不在分支，而在 backbone 的信号来源

---

## 四、300 epoch 完整复现建议

1. 使用原论文参数：`--end_epoch 300 --lr_decay_schedule 150 225`
2. 保留 HSKD bug 修复后的代码
3. batch_size=128（显存充足时），否则 64
4. 预计耗时 ~5.7 小时（RTX 4050 Laptop）
5. 预期最终 Val Top-1 约 74-76%（论文报告水平）

---

## 五、项目文件变更清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `models/resnet_dtskd.py` | 仅注释 | 添加架构说明 |
| `main.py` | Bug Fix | HSKD 单GPU 预测更新 |
| `main.py` | Bug Fix | `--resume` 重复删除 |
| `main.py` | 新增 | CSV 指标记录 |
| `plot.py` | 新增 | 训练可视化（5张图） |
| `experiments/` | 新增 | 两次实验日志+图表 |
